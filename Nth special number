#include<stdio.h>
#include<stdlib.h>

int main() {
  int n, count = 0, num = 0;
  scanf("%d", &n);

  while (count < n) {
    if (num % 10 <= 5) {
      count++; 
    }
    num++; 
  }

  printf("%d\n", num - 1); // Output the special number.

  return 0;
}
