#include <stdio.h>

int main() {
    char string[100];
    scanf("%s", string);

    for (int i = 0; string[i] != '\0'; i++) {
        if (string[i] >= 'A' && string[i] <= 'Z') {
            printf("%c", string[i]);
        }
    }

    return 0;
}
