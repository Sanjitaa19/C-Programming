#include <stdio.h>
#include <string.h> 

int main() {
    char s[1001];
    scanf("%[^\n]", s);
    int count = 1;
    for (int i = 0; s[i] != '\0'; i++) {
        if (s[i] == ' ') {
            count++;
        }
    }
    printf("%d\n", count);
    return 0; 
}
