#include<stdio.h>
#include<stdlib.h>

int main() {
  char s[200];
  char a;
  char b;
  int k=0;
  scanf("%s\n%c\n%c", &s, &a,&b);
  for(int i=0;s[i]!='\0'&&s[i+1]!='\0';i++){
    if(s[i]==a&&s[i+1]==b){
      k++;
    }
  }
  printf("%d",k);
}
